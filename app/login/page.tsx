"use client";
import {FormEvent,useState} from 'react';import Link from 'next/link';import {useRouter} from 'next/navigation';
export default function LoginPage(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setError('');setBusy(true);const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const b=await r.json();setBusy(false);if(!r.ok){setError(b.error||'تعذر تسجيل الدخول');return}router.push('/dashboard')}return <main className="min-h-screen bg-[#fbfaf8] flex items-center justify-center p-5"><div className="w-full max-w-md"><Link href="/" className="block text-center serif text-3xl font-bold mb-8">دعوتي برو</Link><div className="bg-white border border-[#e9e3dc] rounded-[32px] p-7 md:p-9 shadow-sm"><div className="text-center"><p className="gold text-sm font-bold">مرحبًا بعودتك</p><h1 className="serif text-3xl mt-2">تسجيل الدخول</h1><p className="text-sm text-[#756f68] mt-3">أدخل بياناتك لإدارة دعواتك.</p></div><form onSubmit={submit} className="mt-8"><label className="block text-sm"><span className="block mb-2">البريد الإلكتروني</span><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} className="field"/></label><label className="block text-sm mt-5"><span className="block mb-2">كلمة المرور</span><input type="password" required value={password} onChange={e=>setPassword(e.target.value)} className="field"/></label>{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}<button disabled={busy} className="btn btn-primary w-full mt-6">{busy?'جارٍ الدخول…':'دخول'}</button></form><p className="text-center text-sm text-[#756f68] mt-7">ليس لديك حساب؟ <Link href="/register" className="gold font-bold">أنشئ حسابًا</Link></p></div></div></main>}
