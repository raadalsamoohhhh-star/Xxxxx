"use client";
import { FormEvent, useState } from 'react'; import { useRouter } from 'next/navigation'; import Link from 'next/link';
export default function AdminLogin(){ const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [busy,setBusy]=useState(false); const router=useRouter();
 async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const x=await r.json().catch(()=>({}));setBusy(false);if(!r.ok){setError(x.error||'تعذر تسجيل الدخول');return}router.replace('/admin');router.refresh();}
 return <main className="min-h-screen bg-[#181715] text-white flex items-center justify-center p-5"><div className="w-full max-w-md"><Link href="/" className="block text-center serif text-3xl font-bold mb-8">دعوتي برو</Link><div className="rounded-[32px] border border-white/10 bg-white/5 p-8"><p className="text-[#d6b58a] text-sm font-bold">منطقة محمية</p><h1 className="serif text-4xl mt-2">دخول الإدارة</h1><p className="text-white/55 text-sm mt-3">للمشرفين المصرح لهم فقط.</p>{error&&<div className="mt-5 rounded-2xl bg-red-500/10 border border-red-400/20 p-3 text-sm text-red-200">{error}</div>}<form onSubmit={submit} className="mt-8"><label className="block text-sm"><span className="block mb-2 text-white/70">البريد الإداري</span><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="w-full rounded-2xl bg-white/10 border border-white/10 px-4 py-3 outline-none focus:border-[#d6b58a]"/></label><label className="block text-sm mt-5"><span className="block mb-2 text-white/70">كلمة المرور</span><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="w-full rounded-2xl bg-white/10 border border-white/10 px-4 py-3 outline-none focus:border-[#d6b58a]"/></label><button disabled={busy} className="btn w-full mt-6 bg-white text-[#181715]">{busy?'جارٍ الدخول…':'دخول لوحة الإدارة'}</button></form></div></div></main> }
