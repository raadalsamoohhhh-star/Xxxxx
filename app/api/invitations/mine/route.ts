import {NextResponse} from 'next/server';import {db} from '@/lib/server-db';import {getCurrentUser} from '@/lib/auth';export async function GET(){const u=await getCurrentUser();if(!u)return NextResponse.json({error:'غير مصرح'},{status:401});const all=await db.invitations();return NextResponse.json(Object.values(all).filter(x=>x.ownerId===u.id))}
